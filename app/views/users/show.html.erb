<%= provide(:title, @user.first_name )%>
 <div class="row">
   <aside class="col-md-4">
     <section class="user_info">
       <h1>
         <%= @user.first_name%>
        </h1>
        <h1>
         <%= @user.last_name%>
        </h1>
        <p>
        <h3> Matches </h3>
        <%= @user.matches.first.connection.first_name %><br>
        <%= connection_match.first.user.first_name %><br>
        <h3> Business Cards </h3>
        <%= random_user.first_name %>
        #button to call(check mark 'yes') user_clicks_button_to_connect


        <%= @user.matches.first.connection.first_name %>
        <%= connection_match.first.user.first_name %>


        #no button, no button, refresh the business card element

     </section>
   </aside

