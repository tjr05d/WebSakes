<%= form_for @event do |f| %>

<% if @event.errors.any? %>
<div id="error_explantion">
  <h2>
    <%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:
  </h2>
  <ul>
    <%= @event.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :event %><br>
  <%= f.text_field :event %>
</div>

<div class="field">
  <%= f.label :published_on %><br>
  <%= f.text_area :published_on %>
</div>

<div class="field">
<%= f.label :content %><br>
<%= f.text_area :content %>

<div class="actions">
  <%= f.submit %>
</div>

<% end %>

