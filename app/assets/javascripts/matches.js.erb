// var apiKey = @api_key
// // '45406182';
//
// var sessionID = @session.session_id
//  // '1_MX40NTQwNjE4Mn5-MTQ0NzM4MDU1MjkzMX5FL2QwL291ZHljdjVUNG9MK0YrZklBMDJ-fg';
//
// var token = @token
//  // 'T1==';

clicking = function(){

 // var session = OT.initSession(apiKey, sessionID);

 $('#startSession').on('click', function (){
   console.log("session started");
   var session = OT.initSession(apiKey, sessionID );
   session.connect(token, function(error) {
     if (error) {
       console.log(error.message);
     } else {
     // This example assumes that a DOM element with the ID 'publisherElement' exists
       $('#startChat').on('click', function() {
        //  $('#myModal').modal('show');
       var publisherProperties = {width: 200, height:300, name:"Tim's stream"};

       publisher = OT.initPublisher('yourVideo', publisherProperties);

       session.publish(publisher);

       $('#endChat').on('click', function(){
         publisher.destroy();
       });
      });
      }

      session.addEventListener('sessionConnected', function(e){
        session.publish( publisher );
          for (var i = 0; i < e.streams.length; i++) {
            // Make sure we don't subscribe to ourself
            if (streams[i].connection.connectionId == session.connection.connectionId) {
              return;
            }
            var newStream = document.querySelector('theirVideo');

            var stream = document.createElement('div');
            stream.innerHTML = event.data;
            stream.className = event.from.connectionId === session.connection.connectionId ? 'mine' : 'theirs';
            newStream.appendChild(stream);

           });
             });

             session.addEventListener('streamCreated', function(e){
                 for (var i = 0; i < e.streams.length; i++) {
         // Make sure we don't subscribe to ourself
                   if (streams[i].connection.connectionId == session.connection.connectionId) {
                 return;
               }
           // Create the div to put the subscriber element in to
           var newStream = document.querySelector('theirVideo');

           var stream = document.createElement('div');
           stream.innerHTML = event.data;
           stream.className = event.from.connectionId === session.connection.connectionId ? 'mine' : 'theirs';
           newStream.appendChild(stream);

         },

       });

            // div.setAttribute('id', 'stream' + streams[i].streamId);
            // var node = getElementById('textchat')
            // node.appendChild('div');
            // session.subscribe(streams[i], div.id);
        }


      // session.on('streamCreated', function(event) {
      // session.subscribe(event.stream, 'textchat', {
      // insertMode: 'append',
      // width: 400,
      // height: 300
  });
});

  var form = document.querySelector('form');
  var msgTxt = document.querySelector('#msgTxt');

    form.addEventListener('submit', function(event) {
    event.preventDefault();

    session.signal({
      type: 'chat',
      data: msgTxt.value
    },
    function(error) {
      if (!error) {
        msgTxt.value = '';
      }
    });
  });


 var msgHistory = document.querySelector('#history');

   session.on('signal:chat', function(event) {
   var msg = document.createElement('p');
   msg.innerHTML = event.data;
   msg.className = event.from.connectionId === session.connection.connectionId ? 'mine' : 'theirs';
   msgHistory.appendChild(msg);
   msg.scrollIntoView();
  });
    });
})
}
$(document).ready(clicking);
$(document).on('page:load', clicking);
